{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "DatabaseProvider": "Sqlite",
  "PublicBooking": {
    "//": "Incoming events persist as BookingRequests; no placeholder patient/provider is required.",
    "ApiKey": "",
    "//RequireApiKeyForReads": "Set true if this service is ever internet-reachable so GET /api/appointments also requires the API key (prevents leaking contact details stored in Notes).",
    "RequireApiKeyForReads": false
  },
  "ServiceBus": {
    "//": "When ConnectionString is empty, the BookingRequestConsumer stays idle (no broker).",
    "ConnectionString": "",
    "BookingTopic": "booking-requests",
    "BookingSubscription": "scheduling",
    "SchedulingAvailabilityTopic": "scheduling-availability",
    "SchedulingAvailabilitySubscription": "zocdoc"
  }
}
