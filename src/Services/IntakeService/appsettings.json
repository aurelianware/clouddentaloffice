{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "PublicBooking": {
    "//": "Public website booking intake (POST /api/public/booking-requests). Disabled until configured.",
    "Enabled": false,
    "ApiKey": ""
  },
  "ServiceBus": {
    "//": "When ConnectionString is empty, booking events are logged and dropped (no broker).",
    "ConnectionString": "",
    "BookingTopic": "booking-requests"
  }
}
